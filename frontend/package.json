{
  "name": "frontend",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx wrangler deploy",
    "deploy:preview": "npx wrangler versions upload"
  }
}